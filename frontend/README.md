# BidYatra Staff System Frontend

Vanilla HTML/CSS/JavaScript frontend for the Spring Boot API.

## Run
Use any static server from this folder, for example:

```bash
python -m http.server 5500
```

Then open:
http://localhost:5500

The frontend calls:
http://localhost:8080/api

Make sure the Spring Boot backend and MongoDB are running first.
